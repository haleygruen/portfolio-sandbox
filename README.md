# Portfolio Sandbox

A clean, isolated portfolio repo for experimentation and site construction.

## Local commands

```bash
cd ~/portfolio-sandbox
npm install
npm run dev -- --host 0.0.0.0
```

Then open http://localhost:4321
